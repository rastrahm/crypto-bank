# Nginx reverse proxy — Crypto Bank Vault demo
# Copiar a /etc/nginx/sites-available/crypto-bank y ajustar server_name.
# HTTPS: certbot --nginx -d <dominio>

upstream crypto_bank_next {
    server 127.0.0.1:3000;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name demo.example.com;   # ← cambiar

    # Redirect HTTP→HTTPS cuando tengas certs (certbot lo añade solo).
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://crypto_bank_next;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 60s;
    }
}

# Tras certbot, queda un server { listen 443 ssl; ... } similar.
